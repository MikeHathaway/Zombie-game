<!DOCTYPE HTML>

<html lang="en">
<head>

  <link rel="stylesheet" href="/css/style.css" type="text/css">
  <!--
  <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/bootstrap/3.3.1/css/bootstrap.min.css">
  <link rel="stylesheet" href="https://fonts.google.com/?selection.family=Indie+Flower" type="text/css">
  -->
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Mike's Game of Zombies</title>
</head>

<body>
  <div class="container-fluid">

    <a href="https://github.com/MikeHathaway/Zombie-game"><h1 class="link-container">Welcome to Zombieville<h1></a>
    <canvas class="canvas-outline" id="c" width="1800" height="1170"> </canvas>

    <div class="background">
      <p>Rules</p>
      <ol>1:</ol>
      <ol>2:</ol>
      <ol>3:</ol>
    </div>

    <div class="row background">
      <p>Initalize Conditions</p>
      <form>
        <div class="col-xs-4 red">
          <button id="ominousWarning" onclick="startGame()" type="submit">Start Game</button>
        </div><!--
        <div class="col-xs-4">
          <input type="text" placeholder="Number of generations" required>
        </div>
        <div class="col-xs-4">
          <input type="text" placeholder="Percent starting as Zombies" required>
        </div> -->
      </form>
    </div>
  </div>

    <script>
      var c = document.querySelector("#c");
      var ctx = c.getContext("2d");

      function startGame() {
          myGameArea.start();
          document.querySelector('#ominousWarning').onclick = alertFun;
          alert('And so it begins...');
          console.log('And so it begins...');
      }

      function alertFun(){
        alert('And so it begins...');
      }

      var myGameArea = {
          canvas: c,
          start: function() {
              this.canvas.width = canvas.width;
              this.canvas.height = canvas.height;
              this.context = this.canvas.getContext("2d");
              document.body.insertBefore(this.canvas, document.body.childNodes[0]);
          }
      }


      //playing aroInitialize conditionsund objects
//      ctx.fillStyle = "green";
//      ctx.fillRect(200,200,50,50);


  /* Holdover for Udacity canvas class
      var image = new Image();

      image.onload = function(){
        console.log("Loaded image");
        ctx.drawImage(image,150,150,50,50);
      }

      image.src = "2000px-Map_of_USA.svg.png";
  */
    </script>
</body>

</html>
